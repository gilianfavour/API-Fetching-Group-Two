name: nutriblend_group2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'     # ← Changed this line
  # sdk: '^3.11.0'
    sdk: '^3.9.2'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0

  provider: ^6.1.2
  http: ^1.2.1 
  google_fonts: ^6.2.1  # Or latest stable version


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true